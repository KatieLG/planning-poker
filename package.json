{
  "name": "planning-poker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm:dev --workspace=backend\" \"npm:dev --workspace=frontend\"",
    "lint": "concurrently -k -n backend,frontend -c blue,green \"npm:lint --workspace=backend\" \"npm:lint --workspace=frontend\"",
    "format": "concurrently -k -n backend,frontend -c blue,green \"npm:format --workspace=backend\" \"npm:format --workspace=frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
